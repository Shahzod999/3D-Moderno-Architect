import { createTheme } from "@mui/material/styles";

const theme = createTheme({
  palette: {
    primary: {
      main: "rgba(30,26,22)",
    },
    secondary: {
      main: "rgba(50, 43, 36, 0.858)",
    },
  },
});

export default theme;
